<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: Qianbao | 乐者为王]]></title>
  <link href="http://codemany.com/tags/qianbao/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2014-08-04T00:34:54+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[部署Qianbao到Heroku时遇到的一些问题]]></title>
    <link href="http://codemany.com/blog/2013/05/19/some-problems-when-deploying-qianbao-to-heroku/"/>
    <updated>2013-05-19T06:25:33+08:00</updated>
    <id>http://codemany.com/blog/2013/05/19/some-problems-when-deploying-qianbao-to-heroku</id>
    <content type="html"><![CDATA[<p>Heroku现在已经是纯粹的只读PaaS了，也就是说以前还支持的SQlite现在也不能使用了。因此部署到Heroku上的Rails应用需要把使用的数据库改成PostgreSQL，并且要关闭assets的预编译功能。</p>

<p>修改Gemfile，将SQLite换成PostgreSQL：
```</p>

<h1>gem &lsquo;sqlite3&rsquo;</h1>

<p>gem &lsquo;pg&rsquo;
```</p>

<p>在config/application.rb中添加：
<code>
config.assets.initialize_on_precompile = false
</code></p>

<p>股票功能需要导入交割单文件。因为导入后的文本文件不再使用，所以可以把上传路径由public/uploads改为tmp。这样就避免了不能写文件到public目录的问题。</p>

<p>应用上传后运行时出现异常，使用heroku logs -t命令查看日志发现有如下错误：
<code>
Error: column "stocks.share_name" must appear in the GROUP BY clause or be used in an aggregate function
</code></p>

<p>这是因为在controller中有这么一行代码：
<code>
current_user.stocks.select("share_code, share_name, sum(actual_amount) as amount").group("share_code")
</code></p>

<p>在PostgreSQL中这会有问题。比如下面的数据表：
<img src="/uploads/qianbao-stocks-table.png" title="qianbao-stocks-table" ></p>

<p>执行上面的SQL语句后，share_name的值到底是取Ruby呢还是ST Ruby？解决这个问题的方法是使用aggregate函数。
<code>
current_user.stocks.select("share_code, max(share_name) as share_name, sum(actual_amount) as amount").group("share_code")
</code></p>

<p>PostgreSQL还有个问题，就是decimal类型的字段，取出来的值是字符串类型。例如：
<code>
if stock.amount &lt; 0
</code></p>

<p>它会报以下错误：
<code>
ArgumentError (comparison of String with 0 failed)
</code></p>

<p>这个可以通过to_f函数解决：
<code>
if stock.amount.to_f &lt; 0
</code></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[用Rails 3.2打造简单记账应用]]></title>
    <link href="http://codemany.com/blog/2012/04/18/to-create-a-simple-accounting-application-a-using-rails-32/"/>
    <updated>2012-04-18T07:49:27+08:00</updated>
    <id>http://codemany.com/blog/2012/04/18/to-create-a-simple-accounting-application-a-using-rails-32</id>
    <content type="html"><![CDATA[<p>Rails已经到3.2版本了，和以前2.3版本有了很大的不同，决定把原来老版本的记账应用重新写一遍。</p>

<p>我们先来定义一下什么是最简单的记账应用：<br />
1. 可以输入花钱的记录<br />
2. 可以修改输入了的记录<br />
3. 可以删除输入了的记录<br />
4. 可以显示所有的记录</p>

<p>下面我们就开始动手吧！<br />
<code>
rails new qianbao&lt;br /&gt;
cd qianbao&lt;br /&gt;
rails g scaffold entry amount:decimal tags:string effective_date:date&lt;br /&gt;
rails g controller home index&lt;br /&gt;
</code></p>

<p>删除public/index.html，在config/routes.rb中添加根路由：<br />
<code>
root :to =&gt; 'home#index'&lt;br /&gt;
</code></p>

<p>现在要给应用加上一个认证系统（注册、激活、登录、登出），通过devise（<a href="https://github.com/plataformatec/devise%EF%BC%89%E6%8F%92%E4%BB%B6%E5%AE%9E%E7%8E%B0%E3%80%82">https://github.com/plataformatec/devise%EF%BC%89%E6%8F%92%E4%BB%B6%E5%AE%9E%E7%8E%B0%E3%80%82</a></p>

<p>在Gemfile中添加：<br />
<code>
gem 'devise'&lt;br /&gt;
</code></p>

<p>然后执行下列命令：<br />
<code>
bundle install&lt;br /&gt;
rails g devise:install&lt;br /&gt;
rails g devise user&lt;br /&gt;
rails g devise:views&lt;br /&gt;
</code></p>

<p>创建一个migration，把user和entry关联起来：<br />
```
class AddUserToEntry &lt; ActiveRecord::Migration<br />
  def change<br /></p>

<pre><code>add_column :entries, :user_id, :integer

add_index :entries, :user_id&lt;br /&gt;
</code></pre>

<p>  end<br />
end<br />
```</p>

<p>修改Entry和User模型：<br />
```
class Entry &lt; ActiveRecord::Base<br />
  &hellip;<br />
  validates :effective_date, :presence => true<br />
  validates :amount,         :presence => true,<br /></p>

<pre><code>                         :numericality =&gt; {:greater_than =&gt; 0.0},&lt;br /&gt;
                         :format =&gt; {:with =&gt; /^\d+??(?:\.\d{0,2})?$/}&lt;br /&gt;
</code></pre>

<p>  validates :tags,           :presence => true,<br /></p>

<pre><code>                         :length =&gt; { :within =&gt; 1..255 }&lt;br /&gt;
                         &lt;br /&gt;
</code></pre>

<p>  belongs_to :user<br />
```</p>

<p><code>
class User &lt; ActiveRecord::Base&lt;br /&gt;
  ...&lt;br /&gt;
  has_many :users&lt;br /&gt;
</code></p>

<p>修改app/views/home/index.html.erb为如下内容：<br />
```</p>

<h1>Welcom to Qianbao!</h1>


<p>&lt;% if user_signed_in? %><br />
  <p><br /></p>

<pre><code>&lt;strong&gt;&lt;%=link_to current_user.email, edit_user_registration_path %&gt;&lt;/strong&gt;&lt;br /&gt;
&lt;%= link_to 'Logout', destroy_user_session_path, :method =&gt; :delete %&gt;&lt;br /&gt;
</code></pre>

<p>  </p><br />
  &lt;%= link_to &ldquo;All Entries&rdquo;, entries_path %><br />
&lt;% else %><br />
  <p><br /></p>

<pre><code>&lt;strong&gt;You are currently not logged in.&lt;/strong&gt;&lt;br /&gt;
&lt;%= link_to 'Login', new_user_session_path %&gt; or&lt;br /&gt;
&lt;%= link_to 'Sign up', new_user_registration_path %&gt;&lt;br /&gt;
</code></pre>

<p>  </p><br />
&lt;% end %><br />
```</p>

<p>修改app/controller/entries_controller.rb：<br />
```
class EntriesController &lt; ApplicationController<br />
  before_filter :authenticate_user!</p>

<p>  # GET /entries<br />
  # GET /entries.json<br />
  def index<br /></p>

<pre><code>@entries = current_user.entries

respond_to do |format|&lt;br /&gt;
  format.html # index.html.erb&lt;br /&gt;
  format.json { render json: @entries }&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # GET /entries/1<br />
  # GET /entries/1.json<br />
  def show<br /></p>

<pre><code>@entry = current_user.entries.find(params[:id])

respond_to do |format|&lt;br /&gt;
  format.html # show.html.erb&lt;br /&gt;
  format.json { render json: @entry }&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # GET /entries/new<br />
  # GET /entries/new.json<br />
  def new<br /></p>

<pre><code>@entry = current_user.entries.new

respond_to do |format|&lt;br /&gt;
  format.html # new.html.erb&lt;br /&gt;
  format.json { render json: @entry }&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # GET /entries/1/edit<br />
  def edit<br /></p>

<pre><code>@entry = current_user.entries.find(params[:id])&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # POST /entries<br />
  # POST /entries.json<br />
  def create<br /></p>

<pre><code>@entry = current_user.entries.new(params[:entry])&lt;br /&gt;
@entry.user = current_user

respond_to do |format|&lt;br /&gt;
  if @entry.save&lt;br /&gt;
    format.html { redirect_to @entry, notice: 'Entry was successfully created.' }&lt;br /&gt;
    format.json { render json: @entry, status: :created, location: @entry }&lt;br /&gt;
  else&lt;br /&gt;
    format.html { render action: "new" }&lt;br /&gt;
    format.json { render json: @entry.errors, status: :unprocessable_entity }&lt;br /&gt;
  end&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # PUT /entries/1<br />
  # PUT /entries/1.json<br />
  def update<br /></p>

<pre><code>@entry = current_user.entries.find(params[:id])

respond_to do |format|&lt;br /&gt;
  if @entry.update_attributes(params[:entry])&lt;br /&gt;
    format.html { redirect_to @entry, notice: 'Entry was successfully updated.' }&lt;br /&gt;
    format.json { head :no_content }&lt;br /&gt;
  else&lt;br /&gt;
    format.html { render action: "edit" }&lt;br /&gt;
    format.json { render json: @entry.errors, status: :unprocessable_entity }&lt;br /&gt;
  end&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  # DELETE /entries/1<br />
  # DELETE /entries/1.json<br />
  def destroy<br /></p>

<pre><code>@entry = current_user.entries.find(params[:id])&lt;br /&gt;
@entry.destroy

respond_to do |format|&lt;br /&gt;
  format.html { redirect_to entries_url }&lt;br /&gt;
  format.json { head :no_content }&lt;br /&gt;
end&lt;br /&gt;
</code></pre>

<p>  end<br />
end<br />
```</p>

<p>执行以下命令启动应用：<br />
<code>
rake db:migrate&lt;br /&gt;
rails s&lt;br /&gt;
</code></p>

<p>以前的版本用的css框架是<a href="http://blueprintcss.org/">Blueprint</a>，这次使用比它更方便快捷的<a href="https://github.com/twitter/bootstrap">Bootstrap</a>。Bootstrap建立在less上，有个<a href="https://github.com/metaskills/less-rails-bootstrap">less-rails-bootstrap</a>，不过在安装libv8时竟然要提示缺少Python，拜托！我用的是Ruby，要我装Python，啥意思啊！还是找个sass版的Bootstrap吧。</p>

<p>下载<a href="https://github.com/jlong/sass-twitter-bootstrap">sass-twitter-bootstrap</a>：<br />
<code>
git clone https://github.com/jlong/sass-twitter-bootstrap.git&lt;br /&gt;
</code></p>

<p>将sass-twitter-bootstrap/lib中的文件拷贝到app/assets/stylesheets/twitter目录下。然后将app/assets/stylesheets/application.css中的<br />
<code>
*= require_tree .&lt;br /&gt;
[/code]&lt;br /&gt;
修改成&lt;br /&gt;
</code>
*= require twitter/bootstrap<br />
```</p>

<p>再在文件末尾添加：<br />
<code>
body { padding-top: 60px; }&lt;br /&gt;
</code></p>

<p>现在就可以使用Bootstrap来布局美化应用了。</p>

<p><a href="https://github.com/dohkoos/qianbao">代码下载</a>
</p></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[用Rails 2.3打造简单记账软件(12)]]></title>
    <link href="http://codemany.com/blog/2010/01/05/using-the-rails23-to-create-a-simple-accounting-app-12/"/>
    <updated>2010-01-05T08:31:28+08:00</updated>
    <id>http://codemany.com/blog/2010/01/05/using-the-rails23-to-create-a-simple-accounting-app-12</id>
    <content type="html"><![CDATA[<p>在这以前我们把tags作为Entry的一个字段来实现，这样做有很大的局限性，而且还有好多插件可以实现这个功能，现在我们就来把它替换用插件<a href="http://github.com/jviney/acts_as_taggable_on_steroids">acts_as_taggable_on_steroids</a>实现。</p>

<p>安装插件<br />
<code>
script/plugin install git://github.com/jviney/acts_as_taggable_on_steroids.git&lt;br /&gt;
script/generate acts_as_taggable_migration&lt;br /&gt;
rake db:migrate&lt;br /&gt;
</code></p>

<p>在Entry中添加一句代码<br />
```
class Entry &lt; ActiveRecord::Base<br />
  acts_as_taggable</p>

<p>  belongs_to :user<br />
end<br />
```</p>

<p>还要将所有视图中原来的tags字段替换成tag_list字段。<br />
index.html.erb<br />
<code>
&lt;td&gt;&lt; %=h entry.tag_list %&gt;
</code></p>

<p>edit.html.erb<br />
<code>
&lt; %= f.text_field :tag_list %&gt;
</code></p>

<p>new.html.erb<br />
<code>
&lt; %= f.text_field :tag_list %&gt;
</code></p>

<p>还要将entry.rb中的验证字段tags改成tag_list。做完这些插件也就安装完成了。</p>

<p>既然用了插件，那么原来的tags字段就不需要了，写个迁移任务把它处理掉吧！<br />
<code>
script/generate migration remove_tags_from_entry&lt;br /&gt;
</code></p>

<p>迁移代码如下：<br />
```
class RemoveTagsFromEntry &lt; ActiveRecord::Migration<br />
  def self.up<br /></p>

<pre><code>remove_column :entries, :tags&lt;br /&gt;
</code></pre>

<p>  end</p>

<p>  def self.down<br /></p>

<pre><code>add_column :entries, :tags, :string&lt;br /&gt;
</code></pre>

<p>  end<br />
end<br />
```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[用Rails 2.3打造简单记账软件(11)]]></title>
    <link href="http://codemany.com/blog/2010/01/05/using-the-rails23-to-create-a-simple-accounting-app-11/"/>
    <updated>2010-01-05T04:43:29+08:00</updated>
    <id>http://codemany.com/blog/2010/01/05/using-the-rails23-to-create-a-simple-accounting-app-11</id>
    <content type="html"><![CDATA[<p>当网站出现问题时，让它发送一份错误报告到你的邮箱是件挺不错的事，这样你就不必再担心不能及时发现问题了。</p>

<p><a href="http://github.com/rails/exception_notification">exception_notification</a>插件的功能就是当你的Rails应用出错时，它会向指定的邮箱地址发送错误日志。</p>

<p>使用很简单，安装插件后配置一下邮件接受者就可以了。</p>

<p>安装插件<br />
<code>
script/plugin install git://github.com/rails/exception_notification.git&lt;br /&gt;
</code></p>

<p>在config/environment.rb中增加邮件接收者的地址<br />
```
Rails::Initializer.run do |config|<br />
  &hellip;<br />
end</p>

<p>ExceptionNotifier.exception_recipients = %w(<a href="&#109;&#97;&#x69;&#108;&#x74;&#111;&#x3a;&#121;&#111;&#x75;&#x72;&#110;&#97;&#109;&#x65;&#x40;&#x65;&#x78;&#97;&#109;&#x70;&#x6c;&#x65;&#x2e;&#99;&#x6f;&#x6d;">&#x79;&#x6f;&#x75;&#114;&#x6e;&#97;&#x6d;&#101;&#x40;&#101;&#120;&#x61;&#x6d;&#112;&#x6c;&#101;&#46;&#99;&#x6f;&#x6d;</a>)<br />
```</p>

<p>然后告诉Exception Notifier哪些controller出错才发送日志，当然是全部啦：<br />
<code>
class ApplicationController &lt; ActionController::Base&lt;br /&gt;
  include ExceptionNotifiable&lt;br /&gt;
  ...&lt;br /&gt;
end&lt;br /&gt;
</code></p>

<p>Exception Notifier采用Rails中的ActionMailer发送邮件，所以使用的前提是确保ActionMailer可以正常发送邮件。还有就是Exception Notifier默认只在production环境下才会生效，如果想要在development下也生效，需要将development.rb文件中的<br />
<code>
config.action_mailer.raise_delivery_errors = false&lt;br /&gt;
</code></p>

<p>修改为<br />
<code>
config.action_mailer.raise_delivery_errors = true&lt;br /&gt;
</code></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[用Rails 2.3打造简单记账软件(10)]]></title>
    <link href="http://codemany.com/blog/2010/01/04/using-the-rails23-to-create-a-simple-accounting-app-10/"/>
    <updated>2010-01-04T04:34:41+08:00</updated>
    <id>http://codemany.com/blog/2010/01/04/using-the-rails23-to-create-a-simple-accounting-app-10</id>
    <content type="html"><![CDATA[<p>从2.2版本起Rails开始内置支持i18n，因此以后实现国际化/本地化就可以不再需要各种各样的插件了。</p>

<p>Rails默认的locale文件夹是config/locales，假设你要支持中文和英文，那么你需要在这个文件夹下放置zh.yml和en.yml两个文件。</p>

<p>相应的入门教程网上有不少，我也就不多讲了。这里主要说一下如何在记账应用中实现可以让用户指定语言的i18n实现，即当用户选择English，那么界面就切换成英文界面，并且以后打开的页面也是以英文出现，反之亦如此。</p>

<p>首先在header区添加以下代码：<br />
<code>
&lt; %= link_to 'Chinese', :locale =&gt; 'zh' %&gt;
&lt; %= link_to 'English', :locale =&gt; 'en' %&gt;
</code></p>

<p>点击其中某个链接后，浏览器就会传递对应的参数zh或en到后台。</p>

<p>然后在app/controllers/application_controller.rb中添加以下代码：<br />
```
before_filter :set_locale</p>

<p>def set_locale<br />
  I18n.locale = params[:locale]<br />
end<br />
```</p>

<p>不过这里有个问题就是url中必须得带着参数，不然的话用户的选择就会失效。要想使选择达到持续的效果，可以考虑把这些信息保存在session中，改进后的代码如下：<br />
<code>
def set_locale&lt;br /&gt;
  I18n.locale = params[:locale] || session[:locale]&lt;br /&gt;
  session[:locale] = I18n.locale&lt;br /&gt;
end&lt;br /&gt;
</code></p>

<p>现在这样就基本上差不多了。剩下的就是处理第一次访问时的语言，这个可以从http头中的ACCEPT_LANGUAGE参数获取。最终实现的代码：<br />
<code>
def set_locale&lt;br /&gt;
  accept_lang = request.env['HTTP_ACCEPT_LANGUAGE'].scan(/^[a-z]{2}/).first&lt;br /&gt;
  I18n.locale = params[:locale] || session[:locale] || accept_lang || 'zh'&lt;br /&gt;
  session[:locale] = I18n.locale&lt;br /&gt;
end&lt;br /&gt;
</code></p>

<p>不过当用户设置locale后，在新建Entry时会报出以下的异常：<br />
```
can&rsquo;t convert Symbol into String</p>

<p>Extracted source (around line #8):</p>

<pre><code>6:   &lt;p&gt;
7:     &lt; %= t(:effective_date) %&gt;&lt;br /&gt;
8:     &lt; %= f.date_select :effective_date %&gt;
</code></pre>

<p>```</p>

<p>查找资料后发现可以通过在date_select中加入order来解决。但日期下拉列表中的月份还有问题，出现的不是正常月份，而是一些随机数。这个可以在locale文件中加入month_names。<br />
new.html.erb<br />
<code>
&lt;/p&gt;&lt;p&gt;
  &lt; %= t(:effective_date) %&gt;&lt;br /&gt;
  &lt; %= f.date_select :effective_date %&gt;
&lt;/p&gt;
</code></p>

<p>en.yml<br />
```
en:<br />
  date:<br /></p>

<pre><code>month_names: [~, January, February, ..., November, December]&lt;br /&gt;
order: [ :year, :month, :day ]&lt;br /&gt;
</code></pre>

<p>```</p>

<p>zh.yml<br />
```
zh:<br />
  date:<br /></p>

<pre><code>month_names: [~, 一月, 二月, ..., 十一月, 十二月]&lt;br /&gt;
order: [ :year, :month, :day ]&lt;br /&gt;
</code></pre>

<p>```</p>
]]></content>
  </entry>
  
</feed>
