<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: Linux | 乐者为王]]></title>
  <link href="http://codemany.com/tags/linux/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2016-04-30T23:53:37+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[忘记Linux的root密码后]]></title>
    <link href="http://codemany.com/blog/forgot-root-password-for-linux/"/>
    <updated>2004-12-04T18:29:10+08:00</updated>
    <id>http://codemany.com/blog/forgot-root-password-for-linux</id>
    <content type="html"><![CDATA[<p>一不小心忘记了Linux的root密码，只能以普通用户zer0ne的身份进入系统了。</p>

<p>找到Linux的DISC 1安装光盘，从光盘启动，出现boot:引导符后输入（其中，/dev/hda3是Linux系统所在的分区）：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text">linux single root=/dev/hda3 initrd=
</code></pre></div>
<p>进入系统，拷贝/etc目录下的shadow和passwd文件到/home/zer0ne目录下，修改shadow文件的属性。再到 <a href="http://www.openwall.com/john/">http://www.openwall.com/john/</a> 下载一个John the Ripper，编译安装好后输入：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text">./unshadow passwd shadow &gt; passwd.1
</code></pre></div>
<p>接着运行破解：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text">john passwd.1
</code></pre></div>
<p>这样，过了半个多小时我的root密码就回来了。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Linux下JDK 1.5.0的安装和配置]]></title>
    <link href="http://codemany.com/blog/install-and-configure-jdk-150-on-linux/"/>
    <updated>2004-12-01T11:28:09+08:00</updated>
    <id>http://codemany.com/blog/install-and-configure-jdk-150-on-linux</id>
    <content type="html"><![CDATA[<p>先是在网上找了两篇在Linux上安装JDK的文章：<a href="http://www.cnblogs.com/sirsunny/archive/2004/11/22/67029.html">http://www.cnblogs.com/sirsunny/archive/2004/11/22/67029.html</a> 和 <a href="http://dev.csdn.net/article/46/article/46/46877.shtm">http://dev.csdn.net/article/46/article/46/46877.shtm</a> 。</p>

<p>觉得后者写的比较详细，就照着它来了。步骤1、2、3没问题，把JDK安装好了，现在要配置环境变量了，有三种方法，哪种好呢？第1种不方便，换个shell登陆就不行了，第3种也不好，所有登陆用户都能用，那就只能选择第2种方法了。用vi打开.bashrc文件，在文件末尾添加以下内容：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text">set JAVA_HOME=/usr/java/jdk1.5.0
export JAVA_HOME
set PATH=$PATH:$JAVA_HOME/bin
export PATH
set CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
export CLASSPATH
</code></pre></div>
<p>现在文件内容为：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text"># .bashrc
# User specific aliases and functions

# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

set JAVA_HOME=/usr/java/jdk1.5.0
export JAVA_HOME
set PATH=$PATH:$JAVA_HOME/bin
export PATH
set CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
export CLASSPATH
</code></pre></div>
<p>然后重新登陆，用echo $JAVA_HOME查看环境变量，咦，怎么是空的呢？肯定有什么地方出错了，是不是不要什么set啊，试试把它去掉看看，修改后的.bashrc文件内容如下：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text"># .bashrc
# User specific aliases and functions

# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

JAVA_HOME=/usr/java/j2sdk1.5.0
export JAVA_HOME
PATH=$PATH:$JAVA_HOME/bin
export PATH
CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar
export CLASSPATH
</code></pre></div>
<p>再执行echo $JAVA_HOME命令：</p>
<div class="highlight"><pre><code class="language-text" data-lang="text">/usr/java/jdk1.5.0/
</code></pre></div>
<p>果然，去掉set后环境变量就正确了。</p>
]]></content>
  </entry>
  
</feed>
