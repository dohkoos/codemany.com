<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: todolet | 乐者为王]]></title>
  <link href="http://codemany.com/tags/todolet/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2014-08-27T02:16:45+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[todolet - 抽取文档中TODO注释的工具]]></title>
    <link href="http://codemany.com/blog/2006/09/05/todolet-tool-for-taking-todo-from-the-document/"/>
    <updated>2006-09-05T06:40:11+08:00</updated>
    <id>http://codemany.com/blog/2006/09/05/todolet-tool-for-taking-todo-from-the-document</id>
    <content type="html"><![CDATA[<p>todolet是一个建立在<a href="http://jtopas.sourceforge.net/jtopas/">JTopas (Java tokenizer and parser tools)</a>基础上的Ant任务。用来搜集任意文档（不仅仅是Java格式，也可以是XML、Properties等格式）中的TODO注释。目前只支持<code>/**/、//、#、&lt;!-- --&gt;</code>等类型注释。</p>

<p>假如我们在applicationContext.xml里写了一个bean定义，如下所示：</p>

<p><code>
&lt;bean id="hello" class="com.example.lesson.Hello" /&gt;
</code></p>

<p>以后可能要给这个定义添加一些属性，但又怕到时记不住具体是哪个文件，这时候就可以通过在该文件中添加TODO注释来记住它，就像在Java文件中添加TODO注释一样。添加后的样式如下：</p>

<p>```</p>

<!-- TODO: comments -->


<p><bean id="hello" class="com.example.lesson.Hello" />
```</p>

<p>然后就可以编写一个Ant任务来收集这些TODO注释：</p>

<p>```
<taskdef resource="todo.properties" classpath="${lib.dir}/todolet.jar" /></p>

<p><target name="todolet" description="Generate report of todos in specified files"></p>

<pre><code>&lt;property name="todo.report.dir" value="${build.dir}/docs/todo" /&gt;

&lt;empty dir="${todo.report.dir}" /&gt;
&lt;todo destdir="${todo.report.dir}"&gt;
    &lt;fileset dir="${basedir}"&gt;
        &lt;patternset includes="**/*.java" /&gt;
        &lt;patternset includes="**/*.xml" /&gt;
        &lt;patternset includes="**/*.sql" /&gt;
        &lt;patternset includes="**/*.properties" /&gt;
    &lt;/fileset&gt;
&lt;/todo&gt;
</code></pre>

<p></target>
```</p>

<p>执行todolet任务后就会生成一个类似于以下样式的todo.txt文件：</p>

<p><code>
(applicationContext.xml) comments
</code></p>

<p>代码下载：<a href="https://github.com/dohkoos/todolet">https://github.com/dohkoos/todolet</a></p>
]]></content>
  </entry>
  
</feed>
