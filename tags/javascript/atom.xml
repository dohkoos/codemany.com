<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: JavaScript | 乐者为王]]></title>
  <link href="http://codemany.com/tags/javascript/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2014-09-14T22:03:29+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[使用jQuery显示隐藏侧边栏]]></title>
    <link href="http://codemany.com/blog/2012/10/26/show-hide-sidebar-with-jquery/"/>
    <updated>2012-10-26T08:13:14+08:00</updated>
    <id>http://codemany.com/blog/2012/10/26/show-hide-sidebar-with-jquery</id>
    <content type="html"><![CDATA[<p>需要在记账应用使用侧边栏开关功能，在网上找了段代码修改修改，可惜没有找到漂亮的小图标，只好使用>>和&lt;&lt;符号来表示，太丑陋了！</p>

<p>把jquery.toggle.sidebar.js的代码贴上：</p>

<p>```
jQuery(function() {
  var sidebar = $.cookie("sidebar");
  if (sidebar == "hide") {</p>

<pre><code>$('#sidebar').hide();
$(".toggle").text("&gt;&gt;");
</code></pre>

<p>  }</p>

<p>  $(".toggle").click(function() {</p>

<pre><code>if ($('#sidebar').is(':visible')) {
  $(this).text("&gt;&gt;");
  $('#sidebar').hide();
  $.cookie("sidebar", "hide", { expires: 7 });  // expires in 7 days
} else {
  $(this).text("&lt;&lt;");
  $('#sidebar').show();
  $.removeCookie("sidebar");
};
return false;
</code></pre>

<p>  });
});
```</p>

<p>因为jQuery本身不支持Cookie操作，需要使用官方的插件，可以在 <a href="http://plugins.jquery.com/project/Cookie">http://plugins.jquery.com/project/Cookie</a> 或 <a href="https://github.com/carhartl/jquery-cookie">https://github.com/carhartl/jquery-cookie</a> 下载。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[关于input[text]里的光标定位]]></title>
    <link href="http://codemany.com/blog/2008/11/30/cursor-positioning-about-input-text/"/>
    <updated>2008-11-30T02:09:25+08:00</updated>
    <id>http://codemany.com/blog/2008/11/30/cursor-positioning-about-input-text</id>
    <content type="html"><![CDATA[<p>当使用Tab键切换时，想把光标定位在input[text]的首部。在网上找了一些光标定位的资料发现大多数都是用createTextRange来实现的，而且都出自一个实例。可惜的是在Firefox下createTextRange无效。</p>

<p>后来在Firefox的开发者站点上发现有个setSelectionRange方法可以实现这样的功能。使用格式：</p>

<p>```
o.setSelectionRange(start, end);</p>

<p>o：为文本输入框对象
start：为字符串的起始位置
end：为字符串的末位置
```</p>

<p>尝试后发现并不能定位到文本首位，总是全选所有的文本内容，在后面加上获的焦点的命令也不管用。最后还是在国外的一个站点上找到了解决的办法。</p>

<p>下面的代码在Firefox下不能定位到首位，只能选中全部：</p>

<p>```
function setCaretPosition(aCtrl, aPos) {</p>

<pre><code>if (aCtrl.setSelectionRange) {
    aCtrl.setSelectionRange(aPos, aPos);
    aCtrl.focus();
} else if (aCtrl.createTextRange) {
    var rng = aCtrl.createTextRange();
    rng.collapse(true);
    rng.moveStart('character', aPos);
    rng.moveEnd('character', aPos);
    rng.select();
}
</code></pre>

<p>}
```</p>

<p>经过修改后的代码就可以适用于Firefox，IE，Opera的光标定位了：</p>

<p>```
function setCaretPosition(aCtrl, aPos) {</p>

<pre><code>if (aCtrl.setSelectionRange) {
    setTimeout(function() {
        aCtrl.setSelectionRange(aPos, aPos);
        aCtrl.focus();
    }, 0);
} else if (aCtrl.createTextRange) {
    var rng = aCtrl.createTextRange();
    rng.collapse(true);
    rng.moveStart('character', aPos);
    rng.moveEnd('character', aPos);
    rng.select();
}
</code></pre>

<p>}
```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[《Java和JavaScript关于日期计算上的差异》阅读笔记]]></title>
    <link href="http://codemany.com/blog/2008/05/23/java-and-javascript-on-the-date-of-calculation-of-the-difference-in-reading-notes/"/>
    <updated>2008-05-23T13:30:00+08:00</updated>
    <id>http://codemany.com/blog/2008/05/23/java-and-javascript-on-the-date-of-calculation-of-the-difference-in-reading-notes</id>
    <content type="html"><![CDATA[<p>下面是<a href="http://wangchangjun.javaeye.com/blog/170049">Java和JavaScript关于日期计算上的差异</a>作者给出的结论：</p>

<blockquote><p>当进行日期计算的时候，当前日期的月份必须是大月，并且是在这个月的最后一天也就是31号，比如2008-3-31号，当给这个日期加上一个月，得到的日期是个小月就会出现这样的情况。比如在2008-3-31这天加上一月，那因该是4月份，4月就是小月，那Java就是4月30号，JavaScript 就是5月1号，相差一天。当加上2月后，就是5月，5月是大月，那么计算的结果都是5月31号。</p></blockquote>

<p>不过这样的结论还是不能让人明白Java和JavaScript计算日期时相差的一天是怎么来的。</p>

<p>下面是我对Java和JavaScript日期计算差异的一些理解：</p>

<p>2008-2-29加上12个月，那么就是2009-2-29，但2009年是非闰年，它的2月份只有28天，这样就多出来的一天。如何处理这多出来的一天呢？Java和JavaScript做出了不同的理解。Java认为，既然2009年的2月只有28天，那么那多出来的一天就是多余的，应该丢弃，所以Java的计算结果是2009-2-28；而JavaScript则认为这多出来的一天不能丢弃，应该也加上去。那么2009-2-28号加上一天就是2009-3-31号。</p>

<p>同样的，2008-3-31加上一个月在Java中的结果是2008-4-30号，而在JavaScript中则是2008-5-1号。那如果加上的是两个月呢？2008-3-31加上两个月是2008-5-31号，是有效的日期，所以Java和JavaScript的计算结果都是2008-5-31号。</p>

<p>结论：<br />
当进行日期计算时，如果结果日期的date值超出了该月的最大天数值，那么在Java中就会将该date值设为该月的最大天数值；而JavaScript则不但会将该date值设为该月的最大天数值，还会再加上超出的天数值。例如：2009-1-31号加上一个月，Java的结果就是2009-2-28号，JavaScript的结果则是2009-3-3号，2009-2-28加多出来的3(31 - 28)天。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[JavaScript中的undefined]]></title>
    <link href="http://codemany.com/blog/2007/04/20/javascript-undefined/"/>
    <updated>2007-04-20T16:03:06+08:00</updated>
    <id>http://codemany.com/blog/2007/04/20/javascript-undefined</id>
    <content type="html"><![CDATA[<p>JavaScript有6种类型，undefined、null、boolean、number、string和object。其中，null类型只有一个值null。undefined类型也只有一个值，即undefined，当声明的变量没有被赋值的时候，该变量的默认值就是undefined。</p>

<p><code>
var foo;
</code></p>

<p>上面这行代码声明了变量foo，没有赋值。脚本引擎在处理代码的时候会给该变量赋予值undefined。实际上，该行代码等价于</p>

<p><code>
var foo = undefined;
</code></p>

<p>可以用下面的代码测试：</p>

<p><code>
var foo;
alert(foo == undefined);    // true
alert(foo === undefined);    // true
alert(typeof(foo));    // undefined
</code></p>

<p>JavaScript另一个有趣的方面是在使用变量之前不必声明。例如：</p>

<p><code>
var foo = "Hello ";
bar = foo + "world!";
alert(bar);    // Hello world!
</code></p>

<p>变量bar并没有用var运算符定义，这里只是使用了它，就像已经声明过它一样。脚本引擎遇到未声明过的标识符时，用该变量名创建一个全局变量，并将其初始化为指定的值（这里是foo + "world"）。这是该语言的便利之处，不过如果不能紧密跟踪变量，这样做也很危险。考虑下面的代码：</p>

<p><code>
var foo;
// Make sure this variable isn't defined
//var bar;
alert(foo);
alert(bar);
</code></p>

<p>执行的结果是：</p>

<p><img src="/uploads/undefined.png" title="undefined" ></p>

<p><img src="/uploads/undefined-error.png" title="undefined-error" ></p>

<p>alert(bar)出现错误是因为bar被创建成全局变量后没有被初始化，还处在未初始化（uninitialized）状态。但是，typeof运算符并不真正区分这两种值。考虑下面的代码：</p>

<p><code>
var foo;
// Make sure this variable isn't defined
//var bar;
alert(typeof(foo));    // undefined
alert(typeof(bar));    // undefined
</code></p>

<p>前面的代码对两个变量的输出都是“undefined”，即使变量bar在使用前没有被声明。</p>

<p>当函数无明确返回值时，返回的也是值undefined，如下所示：</p>

<p><code>
function foobar() { }
alert(foobar() == undefined);    // true
alert(foobar() === undefined);    // true
</code></p>

<p>ECMAScript认为undefined是从null派生出来的，所以把它们定义为相等的：</p>

<p><code>
alert(null == undefined);    // true
</code></p>

<p>尽管这两个值相等，但它们的含义不同。undefined是声明了变量但未赋值时赋予该变量的值，null则用于表示尚未存在的对象（但从技术上来说，null仍然是原始值）。如果要区分两者，要使用===或typeof运算符。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[JSON in JavaScript]]></title>
    <link href="http://codemany.com/blog/2007/02/09/json-in-javascript/"/>
    <updated>2007-02-09T23:29:14+08:00</updated>
    <id>http://codemany.com/blog/2007/02/09/json-in-javascript</id>
    <content type="html"><![CDATA[<p>英文原文：<a href="http://www.json.org/js.html">http://www.json.org/js.html</a></p>

<p><a href="http://www.crockford.com/javascript">JavaScript</a>是一种通用的编程语言，被当作页面脚本语言引入Netscape Navigator。<a href="http://www.crockford.com/javascript/javascript.html">它仍被广泛地认为是Java的一个子集，但它不是。</a>它是一门有着<a href="http://www.crockford.com/javascript/survey.html">类C语法</a>和<a href="http://www.crockford.com/javascript/inheritance.html">soft objects</a>的类<a href="http://www.crockford.com/javascript/little.html">Scheme</a>语言，JavaScript在<a href="http://www.ecma-international.org/publications/files/ecma-st/ECMA-262.pdf">ECMAScript Language Specification, <del>Third Edition</del>当前版本已经是5.1了，下个版本将是6</a>中被标准化。</p>

<p><a href="http://www.json.org/">JSON</a>是JavaScript对象字面量表示法（object literal notation）的一个子集。因为JSON是JavaScript的一个子集，所以在语言中可以毫不费力地使用它。</p>

<p>```
var myJSONObject = {"bindings": [</p>

<pre><code>    {"ircEvent": "PRIVMSG", "method": "newURI", "regex": "^http://.*"},
    {"ircEvent": "PRIVMSG", "method": "deleteURI", "regex": "^delete.*"},
    {"ircEvent": "PRIVMSG", "method": "randomURI", "regex": "^random.*"}
]
</code></pre>

<p>};
```</p>

<p>在这个例子中，创建了一个包含单个成员“bindings”的对象，该成员又包含了一个拥有三个对象的数组，每个对象含有“ircEvent”，“method”和“regex”三个成员。</p>

<p>成员可以通过点或下标操作符检索。</p>

<p><code>
myJSONObject.bindings[0].method    // "newURI"
</code></p>

<p>你可以使用eval()函数把JSON文本转换成对象，eval()会调用JavaScript编译器。因为JSON是JavaScript的一个真子集（a proper subset），编译器将正确的分析文本并产生一个对象结构。文本必须被包裹在括号内，避免在JavaScript的语法歧义上失误。</p>

<p><code>
var myObject = eval('(' + myJSONtext + ')');
</code></p>

<p>eval函数是非常快的。然而，它可以编译和执行任何JavaScript程序，所以可能会有安全问题。当源代码是可信的与完善的时候才可以使用eval函数。使用JSON语法分析器则安全的多。在基于XMLHttpRequest的web应用中，通讯只被允许朝着提供该页面的同源的方向，所以是可信的。但它可能不胜任。如果服务器在它的JSON编码里不是严格的，或者它没有严谨地校验所有它的输入，然后它可能投递无效的JSON文本，这些文本可能携带危险的脚本。eval函数将执行脚本，释放恶意。</p>

<p>为了抵御这些，应该使用JSON语法分析器。JSON分析器只能识别JSON文本，拒绝所有的脚本。在提供本地JSON支持的浏览器里，JSON分析器也远远快于eval。预计本地JSON支持将会被包含在下一个ECMAScript标准中。</p>

<p><code>
var myObject = JSON.parse(myJSONtext, reviver);
</code></p>

<p>可选的reviver参数是一个函数，它会被最终结果的各个层面上的每个键值调用。每个值都会被reviver函数的返回结果替换。这可以用来把通用对象转变为伪类实例，或者将date字符串转换为Date对象。</p>

<p>```
myData = JSON.parse(text, function (key, value) {</p>

<pre><code>var type;
if (value &amp;&amp; typeof value === 'object') {
    type = value.type;
    if (typeof type === 'string' &amp;&amp; typeof window[type] === 'function') {
        return new (window[type])(value);
    }
}
return value;
</code></pre>

<p>});
```</p>

<p>JSON字符串转化器（stringifier）则作相反的工作，它将JavaScript数据结构转换为JSON文本。JSON不支持循环的数据结构，所以注意不要把循环的结构交给JSON字符串转化器。</p>

<p><code>
var myJSONText = JSON.stringify(myObject, replacer);
</code></p>

<p>如果stringify方法看到某个对象含有toJSON方法，会调用该方法，并且把转化成字符串后的值被返回。这允许一个对象去确定其自己的JSON表示。</p>

<p>字符转化器方法可以接收一个可选的字符串数组。这些字符串用于选取那些将会被包含在JSON文本中的属性。</p>

<p>字符转化器方法可以接收一个可选的replacer函数。该函数将会在结构中每个值的toJSON方法（如果有）后面被调用。它将每个键和值作为参数传递，this将会被绑定到持有键的对象上。返回的值会被字符串化。</p>

<p>没有在JSON中表示的值（例如函数和undefined）会被排除。</p>

<p>非限定的数字被替换为null。要替换成其它的值，你可以使用像这样的replacer函数：</p>

<p>```
function replacer(key, value) {</p>

<pre><code>if (typeof value === 'number' &amp;&amp; !isFinite(value)) {
    return String(value);
}
return value;
</code></pre>

<p>}
```</p>

<p>给JSON.parse一个相应的reviver可以还原那些。</p>

<p><a href="https://github.com/douglascrockford/JSON-js">The open source code of a JSON parser and JSON stringifier is available.</a> When <a href="http://www.crockford.com/javascript/jsmin.html">minified</a> it is less than 2.5K.</p>
]]></content>
  </entry>
  
</feed>
