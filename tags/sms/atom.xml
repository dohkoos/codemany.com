<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: SMS | 乐者为王]]></title>
  <link href="http://codemany.com/tags/sms/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2014-08-04T00:34:54+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[将Nokia 6670的短信导入Hero]]></title>
    <link href="http://codemany.com/blog/2011/06/21/nokia-6670-sms-into-the-hero/"/>
    <updated>2011-06-21T08:43:10+08:00</updated>
    <id>http://codemany.com/blog/2011/06/21/nokia-6670-sms-into-the-hero</id>
    <content type="html"><![CDATA[<p>这事本该N久前就该干了（09年末把手机从Nokia 6670换到HTC Hero），却一直拖着没有做，今天终于把这事作了一个了结。</p>

<p>Nokia下导出的短信是如Messages_Nokia 6670_20091215.xml这样的一个XML文件，文件内容的编码是unicode，格式就像下面这样：
```
&lt;?xml version=&ldquo;1.0&rdquo; encoding=&ldquo;unicode&rdquo;?>
<SMS PhoneModel="Nokia 6670">
  <Message DateTime="2006-09-07 09:32:22"></p>

<pre><code>&lt;Sender&gt;牛金星&lt;/Sender&gt;
&lt;PhoneNumber&gt;+8613988888888&lt;/PhoneNumber&gt;
</code></pre>

<p>   <Location>Inbox</Location></p>

<pre><code>&lt;Text&gt;This is a test!&lt;/Text&gt;
</code></pre>

<p>  </Message>
  &hellip;
  <Message DateTime="2006-09-07 09:32:22"></p>

<pre><code>&lt;Sender&gt;马尔科&lt;/Sender&gt;
&lt;PhoneNumber&gt;+8613966666666&lt;/PhoneNumber&gt;
&lt;Location&gt;Outbox&lt;/Location&gt;
&lt;Text&gt;Hello, world!&lt;/Text&gt;
</code></pre>

<p>  </Message>
</SMS>
```</p>

<p>本来是想写个Android app来直接导入这些备份短信的，不过在实现的过程中发现了SMS Backup&amp;Restore到备份文件和Nokia的备份文件类似产生了做个将Nokia备份文件转换成SMS Backup&amp;Restore的备份文件，又在网上发现了nicholashan的教程<a href="http://bbs.hiapk.com/thread-140564-1-1.html">《将Nokia的短信导入Hero(可能适用所有Android机型)》</a>，坚定了我的这个想法。</p>

<p>分析了一下SMS Backup&amp;Restore备份文件的格式，文件内容的编码是utf-8的：<br />
```
&lt;?xml version=&lsquo;1.0&rsquo; encoding=&lsquo;utf-8&rsquo; standalone=&lsquo;yes&rsquo; ?>
<smses count="1138">
  &lt;sms protocol=&ldquo;0&rdquo; address=&ldquo;10658601&rdquo; date=&ldquo;1260675608871&rdquo;</p>

<pre><code>type="1" subject="null" body="This is a test!" toa="null"
sc_toa="null" service_center="+8613800510543" read="1"
status="-1" locked="0" /&gt;
</code></pre>

<p>  &lt;sms protocol=&ldquo;0&rdquo; address=&ldquo;13566666666&rdquo; date=&ldquo;1260887183844&rdquo;</p>

<pre><code>type="2" subject="null" body="Hello, Hero." toa="null"
sc_toa="null" service_center="null" read="1"
status="-1" locked="0" /&gt;
</code></pre>

<p>  &lt;sms protocol=&ldquo;0&rdquo; address=&ldquo;+8613588888888&rdquo; date=&ldquo;1260887553000&rdquo;</p>

<pre><code>type="1" subject="null" body="ooops!" toa="null"
sc_toa="null" service_center="null" read="1"
status="-1" locked="0" /&gt;
</code></pre>

<p></smses>
```</p>

<p>与转换相关的就address、date、type和body四个属性。address是发信人的号码；date是短信发送日期的timestamp。这点nicholashan的确说错了，后面不需要加什么0的（实际上nicholashan自己也怀疑自己的说法）；type等于1表示别人发给你的短信，如果是2的话就是你发给其他人的短信；<br />
body就是短信内容了。</p>

<p>将Nokia中的时间转换到SMS Backup&amp;Restore的时间可以使用下面的代码：
```
DateFormat dt = new SimpleDateFormat(&ldquo;yyyy-MM-dd HH:mm:ss&rdquo;);
long value = 1355296332000L; // 2012-12-12 12:12:12
Element e = &hellip;
Attribute attr = e.attribute(&ldquo;DateTime&rdquo;);
try {</p>

<pre><code>value = dt.parse(attr.getValue()).getTime();
</code></pre>

<p>} catch (ParseException ex) {
}
```</p>

<p>完整的代码如下（使用dom4j实现XML解析）：
```
package com.codemany.n6670conv;</p>

<p>import java.io.File;
import java.io.FileWriter;
import java.io.IOException;
import java.text.DateFormat;
import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Iterator;
import java.util.List;</p>

<p>import org.dom4j.Attribute;
import org.dom4j.Document;
import org.dom4j.DocumentException;
import org.dom4j.DocumentHelper;
import org.dom4j.Element;
import org.dom4j.Node;
import org.dom4j.io.SAXReader;
import org.dom4j.io.XMLWriter;</p>

<p>public class Main {</p>

<pre><code>public static void main(String[] args) {
    try {
        SAXReader sr = new SAXReader();
        Document doc = sr.read(new File(args[0]));

        generateDocument(doc);
    } catch (DocumentException e) {
        e.printStackTrace();
    }
}

private static void generateDocument(Document doc) {
    List list = doc.selectNodes("//Message");

    Document document = DocumentHelper.createDocument();
    Element smses = document.addElement("smses");
    smses.addAttribute("count", String.valueOf(list.size()));

    DateFormat dt = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    long value = 1355296332000L; // 2012-12-12 12:12:12
    Iterator iter = list.iterator();
    while (iter.hasNext()) {
        Element e = (Element)iter.next();
        Attribute attr = e.attribute("DateTime");
        try {
            value = dt.parse(attr.getValue()).getTime();
        } catch (ParseException ex) {
        }
        Node phone = e.selectSingleNode("PhoneNumber");
        Node location = e.selectSingleNode("Location");
        Node text = e.selectSingleNode("Text");

        Element sms = smses.addElement("sms");
        sms.addAttribute("protocol", "0");
        sms.addAttribute("address", phone.getText());
        sms.addAttribute("date", String.valueOf(value));
        if ("Inbox".equals(location.getText())) {
            sms.addAttribute("type", "1");
        } else if ("Outbox".equals(location.getText())) {
            sms.addAttribute("type", "2");
        }
        sms.addAttribute("subject", "null");
        sms.addAttribute("body", text.getText());
        sms.addAttribute("toa", "null");
        sms.addAttribute("sc_toa", "null");
        sms.addAttribute("service_center", "null");
        sms.addAttribute("read", "1");
        sms.addAttribute("status", "-1");
        sms.addAttribute("locked", "0");
    }
    try {
        File f = new File("output.xml");
        XMLWriter output = new XMLWriter(new FileWriter(f));
        output.write(document);
        output.close();
    } catch (IOException e) {
        System.out.println(e.getMessage());
    }
}
</code></pre>

<p>}
```</p>

<p>代码下载：<a href="https://github.com/dohkoos/N6670Conv">https://github.com/dohkoos/N6670Conv</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[短信分拆算法]]></title>
    <link href="http://codemany.com/blog/2007/11/22/sms-split-algorithm/"/>
    <updated>2007-11-22T21:34:47+08:00</updated>
    <id>http://codemany.com/blog/2007/11/22/sms-split-algorithm</id>
    <content type="html"><![CDATA[<p>今天，突然心血来潮，写了一段分拆短信的代码。代码如下：</p>

<p>```
public class SMSHelper {<br /></p>

<pre><code>private static final int SMS_MAX_LENGTH = 70;

public static List split(String msg) {&lt;br /&gt;
    if (msg.length() &lt;= SMS_MAX_LENGTH) {&lt;br /&gt;
        throw new IllegalArgumentException("被分拆的短信长度必须大于70个字符");&lt;br /&gt;
    }

    List&lt;String&gt; list = new ArrayList&lt;String&gt;();&lt;br /&gt;
    int i = 0, start = 0;&lt;br /&gt;
    while (true) {&lt;br /&gt;
        i++;&lt;br /&gt;
        String prompt = "(第" + i + "条)";&lt;br /&gt;
        int remain = SMS_MAX_LENGTH - prompt.length();&lt;br /&gt;
        if (start + remain &gt; msg.length()) {&lt;br /&gt;
            list.add(prompt + msg.substring(start));&lt;br /&gt;
            break;&lt;br /&gt;
        }&lt;br /&gt;
        list.add(prompt + msg.substring(start, start + remain));&lt;br /&gt;
        start += remain;&lt;br /&gt;
    }&lt;br /&gt;
    return list;&lt;br /&gt;
}&lt;br /&gt;
</code></pre>

<p>}
[/code]
</p></p>
]]></content>
  </entry>
  
</feed>
