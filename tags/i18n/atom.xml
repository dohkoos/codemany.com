<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: i18n | 乐者为王]]></title>
  <link href="http://codemany.com/tags/i18n/atom.xml" rel="self"/>
  <link href="http://codemany.com/"/>
  <updated>2014-08-03T22:17:08+08:00</updated>
  <id>http://codemany.com/</id>
  <author>
    <name><![CDATA[dohkoos]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[设置i18n时date_select出现can't convert Symbol into String错误]]></title>
    <link href="http://codemany.com/blog/2012/04/19/set-up-i18n-date_select-can-t-the-convert-the-symbol-into-the-string-error/"/>
    <updated>2012-04-19T07:56:49+08:00</updated>
    <id>http://codemany.com/blog/2012/04/19/set-up-i18n-date_select-can-t-the-convert-the-symbol-into-the-string-error</id>
    <content type="html"><![CDATA[<p>问题和order参数有关，解决方法有两种：<br />
1、在每次调用date_select时明确指定order参数；<br />
2、your_lang.yml文件中添加以下内容：<br />
```
date:<br />
  order:<br />
    - :year<br />
    - :month<br />
    - :day<br />
[/code]
</p>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[为你的Rails应用加上分页功能]]></title>
    <link href="http://codemany.com/blog/2010/09/09/for-your-rails-application-with-tabs/"/>
    <updated>2010-09-09T09:37:26+08:00</updated>
    <id>http://codemany.com/blog/2010/09/09/for-your-rails-application-with-tabs</id>
    <content type="html"><![CDATA[<p><p>安装<a href="http://github.com/mislav/will_paginate.git">will_paginate</a>插件：<br />
<code>
script/plugin install git://github.com/mislav/will_paginate.git&lt;br /&gt;
</code></p>

<p><p>安裝好插件后，在action中將本來的find方法<br />
<code>
@contacts = Contact.all&lt;br /&gt;
</code></p>

<p><p>改为<br />
<code>
@contacts = Contact.paginate(:page =&gt; params[:page])&lt;br /&gt;
</code></p>

<p><p>然后在action对应的view最后加入以下代码：<br />
[code lang=&ldquo;ruby&rdquo;]
&lt;%= will_paginate @contacts %>
```</p>

<p><p>现在便可以使用分页功能了，该语句会产生如下的HTML代码：<br />
[code lang=&ldquo;xml&rdquo;]
<div class="pagination">
  <span class="disabled prev_page">&laquo; Previous</span>
  <span class="current">1</span>
  <a href="http://codemany.com/contacts?page=2&s=" rel="next">2</a>
  <a href="http://codemany.com/contacts?page=2&s=" class="next_page" rel="next">Next &raquo;</a>
</div>
```</p>

<p><p>下面加入搜索功能，在view的适当位置加入:<br />
[code lang=&ldquo;ruby&rdquo;]
&lt;% form_tag contacts_path, :method => &lsquo;get&rsquo; do %>
<p>
  &lt;%= text_field_tag :s, params[:s] %>
  &lt;%= submit_tag &ldquo;Search&rdquo;, :name => nil %>
</p>
&lt;% end %>
```</p>

<p><p>并且将action中的代码修改为：<br />
<code>
@contacts = Contact.paginate(&lt;br /&gt;
  :page =&gt; params[:page],&lt;br /&gt;
  :per_page =&gt; 5,&lt;br /&gt;
  :conditions =&gt; ["name like ?", "%#{params[:s]}%"])&lt;br /&gt;
</code></p>

<p><p>试着进行搜索，可以看到搜索结果也很好的进行了分页。</p>

<p><p>此外will_paginate还提供一些分页统计信息：<br />
<code>
Total entries: &lt;%= @contacts.total_entries %&gt;
Total pages: &lt;%= @contacts.total_pages %&gt;
Current page: &lt;%= @contacts.current_page %&gt;
</code></p>

<p><p>最后加上will_paginate推荐的css：<br />
```
.pagination {<br />
  padding: 3px;<br />
  margin: 3px;<br />
}</p>

<p><p>.pagination a {<br />
  padding: 2px 5px 2px 5px;<br />
  margin: 2px;<br />
  border: 1px solid #aaaadd;<br />
  text-decoration: none;<br />
  color: #000099;<br />
}</p>

<p><p>.pagination a:hover, .pagination a:active {<br />
  border: 1px solid #000099;<br />
  color: #000;<br />
}</p>

<p><p>.pagination span.current {<br />
  padding: 2px 5px 2px 5px;<br />
  margin: 2px;<br />
  border: 1px solid #000099;<br />
  font-weight: bold;<br />
  background-color: #000099;<br />
  color: #FFF;<br />
}</p>

<p><p>.pagination span.disabled {<br />
  padding: 2px 5px 2px 5px;<br />
  margin: 2px;<br />
  border: 1px solid #eee;<br />
  color: #ddd;<br />
}
```</p>

<p><p>在写代码的时候碰到一个问题，网上的文章都使用page_count来表示总的分页数，但我在使用时出现了undefined method &lsquo;page_count'错误，把page_count换成total_pages就好了。</p>

<p><p>2011/1/4更新<br />
这里使用的是2.3.15版本的will_paginate，暂时还不支持i18n，不过可以通过在app/helps/application_helper.rb中添加以下代码来实现：<br />
```
include WillPaginate::ViewHelpers</p>

<p><p>def will_paginate_with_i18n(collection, options = {})<br />
  will_paginate_without_i18n(collection, options.merge(<br /></p>

<pre><code>                    :previous_label =&gt; I18n.t(:previous, :default =&gt; 'Previous'),&lt;br /&gt;
                    :next_label =&gt; I18n.t(:next), :default =&gt; 'Next'))&lt;br /&gt;
</code></pre>

<p>  end</p>

<p><p>alias_method_chain :will_paginate, :i18n<br />
```</p>

<p><p>然后在config/locales/zh.yml中添加：<br />
```
next: &ldquo;后一页&rdquo;<br />
previous: &ldquo;前一页&rdquo;<br />
[/code]
</p></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[用Rails 2.3打造简单记账软件(10)]]></title>
    <link href="http://codemany.com/blog/2010/01/04/using-the-rails23-to-create-a-simple-accounting-app-10/"/>
    <updated>2010-01-04T04:34:41+08:00</updated>
    <id>http://codemany.com/blog/2010/01/04/using-the-rails23-to-create-a-simple-accounting-app-10</id>
    <content type="html"><![CDATA[<p><p>从2.2版本起Rails开始内置支持i18n，因此以后实现国际化/本地化就可以不再需要各种各样的插件了。</p>

<p><p>Rails默认的locale文件夹是config/locales，假设你要支持中文和英文，那么你需要在这个文件夹下放置zh.yml和en.yml两个文件。</p>

<p><p>相应的入门教程网上有不少，我也就不多讲了。这里主要说一下如何在记账应用中实现可以让用户指定语言的i18n实现，即当用户选择English，那么界面就切换成英文界面，并且以后打开的页面也是以英文出现，反之亦如此。</p>

<p><p>首先在header区添加以下代码：<br />
[code lang=&ldquo;ruby&rdquo;]
&lt; %= link_to &lsquo;Chinese&rsquo;, :locale => &lsquo;zh&rsquo; %>
&lt; %= link_to &lsquo;English&rsquo;, :locale => &lsquo;en&rsquo; %>
```</p>

<p><p>点击其中某个链接后，浏览器就会传递对应的参数zh或en到后台。</p>

<p><p>然后在app/controllers/application_controller.rb中添加以下代码：<br />
```
before_filter :set_locale</p>

<p><p>def set_locale<br />
  I18n.locale = params[:locale]<br />
end<br />
```</p>

<p><p>不过这里有个问题就是url中必须得带着参数，不然的话用户的选择就会失效。要想使选择达到持续的效果，可以考虑把这些信息保存在session中，改进后的代码如下：<br />
<code>
def set_locale&lt;br /&gt;
  I18n.locale = params[:locale] || session[:locale]&lt;br /&gt;
  session[:locale] = I18n.locale&lt;br /&gt;
end&lt;br /&gt;
</code></p>

<p><p>现在这样就基本上差不多了。剩下的就是处理第一次访问时的语言，这个可以从http头中的ACCEPT_LANGUAGE参数获取。最终实现的代码：<br />
<code>
def set_locale&lt;br /&gt;
  accept_lang = request.env['HTTP_ACCEPT_LANGUAGE'].scan(/^[a-z]{2}/).first&lt;br /&gt;
  I18n.locale = params[:locale] || session[:locale] || accept_lang || 'zh'&lt;br /&gt;
  session[:locale] = I18n.locale&lt;br /&gt;
end&lt;br /&gt;
</code></p>

<p><p>不过当用户设置locale后，在新建Entry时会报出以下的异常：<br />
```
can&rsquo;t convert Symbol into String</p>

<p><p>Extracted source (around line #8):</p>

<p><p>    6:   <p></p>

<pre><code>7:     &lt; %= t(:effective_date) %&gt;&lt;br /&gt;
8:     &lt; %= f.date_select :effective_date %&gt;
</code></pre>

<p>```</p>

<p><p>查找资料后发现可以通过在date_select中加入order来解决。但日期下拉列表中的月份还有问题，出现的不是正常月份，而是一些随机数。这个可以在locale文件中加入month_names。<br />
new.html.erb<br />
[code lang=&ldquo;ruby&rdquo;]
</p><p>
  &lt; %= t(:effective_date) %><br />
  &lt; %= f.date_select :effective_date %>
</p>
```</p>

<p><p>en.yml<br />
```
en:<br />
  date:<br /></p>

<pre><code>month_names: [~, January, February, ..., November, December]&lt;br /&gt;
order: [ :year, :month, :day ]&lt;br /&gt;
</code></pre>

<p>```</p>

<p><p>zh.yml<br />
```
zh:<br />
  date:<br /></p>

<pre><code>month_names: [~, 一月, 二月, ..., 十一月, 十二月]&lt;br /&gt;
order: [ :year, :month, :day ]&lt;br /&gt;
</code></pre>

<p>```</p>
]]></content>
  </entry>
  
</feed>
